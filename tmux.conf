# set the prefix to be ctrl+w
set -g prefix C-w
unbind-key C-b
bind-key C-w send-prefix

# move around panes using hjkl
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
